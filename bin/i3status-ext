#!/usr/bin/env bash

i3status | while :
do
    read line
    rstat=$(git-multi-status ~/.dotfiles ~/workspace/* ~/workspace/drafts/* ~/workspace/sws-twitter-app/*)
    echo "$rstat | $line" || exit 1
done

