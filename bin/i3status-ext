#!/bin/bash

i3status | while :
do
    read line
    rstat=$( git-multi-status $( cat ~/.repo-dirs ) )
    vol=$(sound-ctrl vol)
    mute=$(sound-ctrl ismute)
    echo "$rstat | Mute: $mute, $vol | $line" || exit 1
done

