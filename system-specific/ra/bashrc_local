# Setup virtualenvs
. "$myrc_d/virtualenvwrapper-arch.sh"

# Setup auto-switch-venv
. "$myrc_d/auto-switch-venv.sh"

# Setup program specific directories
. "$myrc_d/lang-path.sh"

# Git commit happy music
. "$myrc_d/git-commit-happy.sh"

# KGP Proxies
. "$myrc_d/kgp-proxies.sh"

export MY_SOUND_SYSTEM=pulse:0

alias ssh-add-twitter-app="ssh-add $HOME_SDOCS/dotfiles-x/ssh-keys/labda-key-rsa"

export _JAVA_OPTIONS='-Dawt.useSystemAAFontSettings=gasp -Dswing.aatext=true -Dswing.defaultlaf=com.sun.java.swing.plaf.gtk.GTKLookAndFeel'
